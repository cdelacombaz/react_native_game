import React from 'react';
import { View, StyleSheet } from 'react-native';

const Component = props => {
  return (
    <View styles={styles.mainContainer}></View>
  );
};

const styles = StyleSheet.create({
  mainContainer: {}
});

export default Component;
